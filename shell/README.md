+ **batch_install.sh** is a batch package installer.
+ **next_quote_variety.sh** sets the next quote in Variety.
+ **set_proxy.sh** automates setting up proxy.
+ **sudo.sh** checks if the script was run as root.
